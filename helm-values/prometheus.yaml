alertmanager:
  enabled: false

server:
  global:
    scrape_interval: 15s
  extraScrapeConfigs: |
    - job_name: 'otel-collector'
      static_configs:
        - targets: ['otel-collector.observability.svc.cluster.local:9464']
